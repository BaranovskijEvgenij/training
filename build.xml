<project name="training" default="help" basedir=".">
       <target name="help">
    	<echo>
    	  Help for the build script...
       </echo>
      		 <echo>
		     /if you write "ant build"  then  Create a folder "build.dir/buildTT" 
		     /if you write "ant clean" then deleted folder "build.dir/buildTT"
		     /if you write "ant run" then compiles Java files 
	        </echo>
      </target>

  <target name="clean">
    	<delete dir="buildTT"/>
  </target>
  
 		 <target name="build" depends="clean">
 		      <mkdir dir="buildTT"/> 
		</target>	       
         
     <target name="run" depends="build">
      <javac srcdir="." destdir="buildTT"/>
    </target>
</project>